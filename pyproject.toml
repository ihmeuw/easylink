[build-system]
requires = ["packaging", "setuptools"]

[tool.setuptools_scm]

[tool.black]
line_length = 94

[tool.isort]
profile = "black"

[tool.coverage.run]
source = ["easylink"]

[tool.coverage.report]
show_missing = true